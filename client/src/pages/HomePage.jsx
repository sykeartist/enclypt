
import Home from '../components/Home';

const HomePage = () => {
    return (
        <div>
            {/* <Suspense fallback={<Loader />}>
                <Home />
            </Suspense> */}
            <Home />
        </div>
    );
};

export default HomePage;